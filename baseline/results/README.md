# Baseline Results

This directory stores baseline agent run results.

Results are JSON files with the format:
```
baseline_results_YYYYMMDD_HHMMSS.json
```

These files are git-ignored to prevent large/sensitive data from being committed.
